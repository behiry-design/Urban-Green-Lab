# NEVER commit this — it holds every team's real device token in plaintext.
# Anyone with these tokens can write fake sensor data or delete a team's
# photos in Supabase, so this file must stay off GitHub entirely, even in a
# private repo.
kit_roster.csv

# Build artifacts / working files, not source
Greenhouse_Training_Platform.zip
*.zip

# Python
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db

# Downloaded model weights for analyze_photos.py — large binary, and anyone
# running the script fetches it fresh per the setup instructions in that
# file's own docstring, so it doesn't belong in the repo.
swarm_model/model/

# One-off patch scripts used to apply fixes during development — not part
# of the project itself.
patch_*.py
